version: "3.9"

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: monitoring

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - db